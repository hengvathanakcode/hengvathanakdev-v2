import { Link } from "react-router-dom"

const Allhighlight = (props) => {
    const { date, title, program, description, images } = props;
    return (    
        <>
            
        </>
    );
};

export default Allhighlight;